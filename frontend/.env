VITE_API_URL=http://localhost:4000/api 

# This value should be dynamic. If you want to go hardway you have to change the backend ip and build the image.
# Easy way is passing the deploy.yaml as env
# env:
   - name: VITE_API_URL
     value: "http://YOUR_BACKEND_IP:4000/api"
